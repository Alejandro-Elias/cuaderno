import React from 'react'
import styles from '@/src/app/viewInfo/viewInfo.module.css'

export const ViewInfo = () => {
  return (
    <div className={`container ${styles.main}`}>viewInfo</div>
  )
}

export default ViewInfo
